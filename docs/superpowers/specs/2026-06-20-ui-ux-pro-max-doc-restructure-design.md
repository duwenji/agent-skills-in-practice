# ui-ux-pro-max ドキュメント再構成 設計仕様

**日付**: 2026-06-20  
**対象ファイル**: `docs/03-frameworks/03-3b-ui-ux-pro-max.md`  
**目的**: 冒頭の価値訴求を強化し、読者が「これは私の問題の解決策だ」と感じる物語構造に再構成する

---

## 読者プロフィール

- Copilot を使い始めているが、生成 UI が粗削りがち
- React などフロントエンドの基礎はある
- AI ツールの使い方を洗練させたい中級者

## フックスタイル

共感型：3シーン（スクリーンリーダーで読めない / モバイル崩れ / コントラスト不足）で始める

---

## 新しい構成

### ① 共感フック — 3シーン（冒頭）

冒頭に「Copilot が生成したが見えない問題が残っている」3つの具体シーンを並べる。

**シーン 1 — スクリーンリーダーで読めない画像**
```
Copilot がロゴを実装した。
<img src="/logo.png" />
動く。表示される。でも alt 属性がない。
スクリーンリーダーは「image」と読み上げるだけだ。
```

**シーン 2 — モバイルで崩れるナビ**
```
デスクトップでは完璧に見える。
375px のスマートフォンで確認したら、
ナビゲーションが画面の右側に切れていた。
```

**シーン 3 — 目には見えないコントラスト不足**
```
背景 #333 に白文字。おしゃれで読みやすい——
少なくとも自分には。
コントラスト比は 3.5:1。WCAG AA の基準は 4.5:1 だった。
```

締めの一文:「Copilot はコードを書く。でも、誰かがこれを使えるか、は教えてくれない。」

### ② ui-ux-pro-max とは（`## ui-ux-pro-max とは`）

コミュニティ（nextlevelbuilder）が開発した UI/UX 監査スキルの説明。

**含める内容:**
- 4観点の概要（アクセシビリティ・レスポンシブ・ユーザビリティ・ビジュアル）の一文説明
- 特徴テーブル（提供元・動作環境・呼び出し方・スキル種別・監査観点 の5行）

### ③ 同じコンポーネントで何が変わるのか（`## 同じコンポーネントで何が変わるのか`）

Copilot チャット出力の Before/After で対比（独立した `##` セクション）。

**Before — @ui-ux-pro-max なし:**
```
あなた: Header コンポーネントを実装して。

Copilot: わかりました。

function Header() {
  return (
    <header style={{background: '#333', color: 'white'}}>
      <img src="/logo.png" />
      <nav>
        <a href="/">Home</a>
        <a href="/about">About</a>
      </nav>
    </header>
  );
}
```

**After — @ui-ux-pro-max あり:**
```
あなた: @ui-ux-pro-max
フレームワーク: React
[上記 Header コード]

Copilot: 監査結果（スコア: 65/100）

🔴 Critical × 2
  A11Y-1: <img> に alt 属性がない（WCAG 1.1.1）
  A11Y-2: コントラスト比 3.5:1（基準: 4.5:1）

🟡 Major × 1
  RSP-1: ナビが 375px でオーバーフロー

Quick Wins:
  alt="会社ロゴ" を追加（5分）
  ハンバーガーメニュー化（30分）
```

### ④ 4つの監査観点（`## 4つの監査観点`）

現行の `### 4つの監査観点` テーブルを独立した `##` セクションに昇格。

冒頭に一文追加:「必要なときに参照してください。最初から全部覚える必要はありません。」

テーブル（観点・説明・チェック内容 の3列、4行）をそのまま維持。

### ⑤ 入力パラメータ（`## 入力パラメータ`）

現行の `### 入力パラメータ` テーブルを `##` に昇格して維持（6パラメータ全行）。

### ⑥ 出力スキーマ（`## 出力スキーマ`）

現行の `### 出力スキーマ` JSON と直後の説明文を `##` に昇格して維持。

### ⑦ 使い方（`## 使い方`）

現行の `## 使い方` を維持（呼び出し方の Header コード例・監査レベル変更例）。

### ⑧ 次のステップ（`## 次のステップ`）

現行のリンクをそのまま維持。

---

## 保持必須コンテンツ（ゼロ削除）

- `@ui-ux-pro-max` 呼び出し例コードブロック（Header コンポーネント含む）
- 特徴テーブル（提供元・動作環境・呼び出し方・スキル種別・監査観点 の5行）
- 4観点テーブル（観点・説明・チェック内容 の3列、4行）
- 入力パラメータテーブル（6パラメータ全行: component_code, framework, audit_focus, wcag_level, viewport_sizes, include_remediation）
- 出力スキーマ JSON（summary / categories / quick_wins）+ 直後の説明文
- `wcag_level=AAA` の監査レベル変更例
- `audit_focus=["responsive"]` のフォーカス絞り込み例
- 次のステップリンク（04-problem-skill-mapping.md, ../04-skills-in-practice/05-ui-ux-pro-max.md）

## 追加するコンテンツ

- ① 3シーン共感フック（スクリーンリーダー / モバイル崩れ / コントラスト不足）
- ③ Before/After 比較（チャット出力形式）

## セクション構造変更

- `## 詳細説明` 廃止 → `## 4つの監査観点`、`## 入力パラメータ`、`## 出力スキーマ` の3セクションに分割昇格
- `### 4つの監査観点` → `##` に昇格
- `### 入力パラメータ` → `##` に昇格
- `### 出力スキーマ` → `##` に昇格
- `## 同じコンポーネントで何が変わるのか` を独立した新規 `##` セクションとして追加
